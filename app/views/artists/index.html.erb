<div class="artist">
  <h1>Artists</h1>
  <br />
  <div class="ui divider">
    <br />
    <%= link_to 'Create a new Artist', new_artist_path %>
  <div class="ui divider">
  <br />

  <div class="ui relaxed divided list">
    <% @artists.each do |artist| %>
    <div class="item">
      <div class="content">
        <li class="header"><%= link_to artist.name, artist %></li>
      </div>
    </div>
  <% end %>
  </div>
</div>
